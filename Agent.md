# Agent 工作规约

本文件只记录长期有效的开发约束，不重复维护项目状态、接口清单和外部协议全文。

开始任务前，按需阅读：

- `README.md`：当前项目状态和运行方式。
- `architecture_guide.md`：总体架构和目录边界。
- `implementation_plan.md`：阶段计划。
- `docs/API.md`：现有 HTTP API。
- `apps/web/DEVELOPMENT.md`：前端目录、依赖、编码、安全和验证规范。
- 各模块下的 `README.md`：模块内约定。

用户最新指令优先于本文件；本文件优先于旧计划和旧说明。

## 1. 项目边界

项目采用模块化单体架构，保持简单，不主动拆微服务或引入重型基础设施。

- `apps/api`：HTTP 服务入口和路由组装。
- `apps/web`：前端应用。
- `internal/`：后端业务模块和基础能力。
- `pkg/jwxt`：独立的教务系统 SDK，不依赖仓库根目录下的业务代码。
- `pkg/jwxt/internal/`：SDK 私有实现，按登录、成绩、课表等职责分包。

外部调用者只使用 `pkg/jwxt` 的公开 API，不直接依赖其 `internal` 实现。

## 2. 编码原则

1. 只实现当前明确需求，不预设未来功能。
2. 优先使用直观、短小、可读的实现，不为了“健壮性”堆积兜底分支。
3. 一个函数只承担一个明确职责；函数过长或混合多个职责时再拆分。
4. 按业务域组织代码，不把无关逻辑堆进同一文件，也不为分层制造大量空壳包。
5. 只在用户输入、网络、数据库、文件等外部边界做必要校验。
6. 优先复用现有代码，不创建重复模块，不顺手重构无关代码。
7. 未经要求，不新增权限体系、缓存、限流、重试、日志框架或抽象层。
8. 标准库或现有依赖能完成时，不引入新依赖。
9. 不随意修改公开结构体、接口格式、数据库结构和模块边界。
10. 如果额外设计会改变需求范围，先说明理由并询问用户。

## 3. 中文注释

注释用于解释业务意图和不直观的规则，不复述语法。

以下内容必须写清楚中文注释：

- 外部系统的请求顺序、必要参数及参数来源。
- 登录、会话、跳转和状态判断依据。
- HTML、JavaScript、JSON 等外部响应的解析依据。
- 索引、位串、日期、教学周、节次等换算规则。
- 不同响应数据之间的关联方式。
- 容易被误删或误改的兼容处理。

注释说明“为什么这样做”和“依据是什么”，实现变化时同步更新。

## 4. 外部系统取证

涉及 CAS、EAMS、一网通办等真实系统时：

1. 只把用户提供的信息、真实请求结果和当前代码行为作为实现依据。
2. 无法确认的事实不得自行补全；当前环境不能直接获取时，向用户索取最小必要证据。
3. 当前环境能够复现或取证时，由 Agent 直接完成，不让用户反复执行同一操作。
4. 用户授权使用已登录的浏览器会话时，可以检查真实流程和页面结果，但不得读取认证存储或敏感值。
5. 调试时同时核对前置请求顺序、Cookie 状态、页面初始化上下文和最终请求，不能只比较 URL、Header 或表单。
6. 每次只验证一个变量，不同时提交多个猜测性修改。
7. 远端通用错误页只是故障现象，不能直接当作根因。
8. 根因必须有可重复证据：相同账号、目标和环境下，修改前稳定失败，修改后稳定成功。
9. 单元测试和模拟响应只能证明本地逻辑，不能证明真实学校系统兼容。
10. 交付前删除临时诊断入口和临时测试代码。

## 5. 敏感信息与会话

任何代码、日志、测试、终端输出和错误响应都不得包含：

- 明文密码、Cookie 值、Session 值、Token、密钥或 CAS ticket。
- 完整 `service` 参数、完整重定向 URL 或含敏感查询参数的 URL。
- 账号与密码的组合信息。
- 认证页面或可能包含认证信息的完整响应正文。

可以记录操作名、host、path、状态码、Content-Type、响应大小和不含敏感信息的必要错误正文。

会话规则：

1. 每个用户的教务会话使用独立 `Client` 和 `CookieJar`，禁止跨用户共享。
2. 不记录、不返回 Cookie 值；诊断时只记录 Cookie 名称或是否存在。
3. 教务密码需要持久化时必须加密，密钥通过环境变量提供，禁止明文入库或写入仓库。
4. 外部请求必须受 `context` 和超时控制。
5. 未经用户要求，不增加自动重试或高频批量请求。
6. 每个学号只保留一个应用 Session，新登录覆盖旧 Session；SQLite 只保存 Token 哈希。
7. 热请求使用 Token 哈希直接查找内存 `clientEntry`，不重复查询 SQLite。
8. 同一用户的教务请求必须串行操作其 `Client`；Client 空闲 3 分钟后释放。

## 6. 职责分层

| 职责 | 内容 |
|---|---|
| handler | 接收请求、必要参数校验、调用 service、返回统一响应 |
| service | 编排业务流程和会话续期 |
| repository | 数据库访问，不承载业务判断 |
| client | 调用学校等外部系统 |
| parser | 纯解析外部数据，不处理登录、权限或数据库 |

handler 不直接访问数据库或学校系统；service 不堆积 HTML 解析和复杂 SQL；SDK 不依赖业务层。

## 7. 错误与日志

1. 所有外部调用、解析、数据库和配置错误都必须处理并向上返回。
2. 使用 `%w` 包装错误，保留操作阶段和原始原因，支持 `errors.Is`。
3. 不用笼统错误隐藏真实失败位置；安全范围内保留 host、path、状态码和远端错误摘要。
4. 日志只记录排查所需信息，不记录请求体、Cookie、密码或完整跳转地址。
5. 对外 API 沿用项目现有统一响应格式，不把敏感底层信息返回前端。

## 8. 测试与验证

1. parser 等纯逻辑优先使用固定样本编写离线单元测试。
2. 网络集成测试必须通过 build tag 或环境变量显式开启，默认不访问学校系统。
3. 测试代码和样本中不得出现真实账号、密码、Cookie 或 ticket。
4. 修复外部兼容问题时，同时保留离线回归测试和一次真实链路验证结果。

Go 代码按修改范围执行：

```bash
go test ./...
go vet ./...
```

前端代码按修改范围执行：

```bash
cd apps/web
pnpm run check
```

只修改文档时执行 `git diff --check` 即可。

## 9. 执行流程

1. 阅读任务相关代码、文档和当前工作区状态。
2. 区分已确认事实、未知信息和待验证假设。
3. 确定最小修改范围，只修改当前任务涉及的文件。
4. 实现后运行与风险相称的测试；真实外部系统问题必须补真实验证。
5. 同步更新受影响的注释和文档。
6. 交付时说明修改内容、验证结果和仍未确认的事项。

除非用户明确要求，不实现抢课、批量抓取、微服务、消息队列、多级缓存、CQRS、事件溯源或其他与当前需求无关的扩展。
