# Agent 工作指南

本文件用于指导后续进入本仓库的 AI Agent 或开发者执行任务。

请先阅读：

1. `architecture_guide.md`：项目总体架构。
2. `implementation_plan.md`：第一阶段执行计划。
3. `README.md`：当前仓库状态。

如果本文件、架构文档、执行计划与用户最新指令冲突，以用户最新指令为准。

## 1. 项目定位

本项目是成都信息工程大学 Campus Assistant 后端项目，是一个面向学生使用的教务助手系统。

核心目标：

- 稳定。
- 安全。
- 清晰。
- 可维护。

第一版采用模块化单体架构，不拆微服务，不上 Kafka，不提前引入过重基础设施。

核心能力：

- 安全访问学校 CAS / EAMS。
- 获取课表、成绩、考试等教务数据。
- 后续通过 API、缓存、数据库向前端提供稳定服务。

当前最重要的模块是：

```text
pkg/jwxt
```

它是独立 JWXT SDK，只负责学校系统访问，不依赖任何业务层代码。

## 2. 当前阶段

当前仓库已完成第一阶段 JWXT 登录闭环。

已经有：

- 架构指导文档。
- 第一阶段计划执行书。
- `pkg/jwxt` CAS/EAMS 登录 SDK。
- `cmd/jwxt-demo` 登录验证入口。
- 已真实验证的一网通办、CAS 桥接页和 EAMS 登录流程。
- 已完成离线测试、等待真实验证的指定学期成绩查询。
- `apps/api` 最小 Hertz API，提供教务登录、登录状态、退出、学期列表和成绩查询。
- `apps/web` Vue 3 PWA，包含登录、课表、工具、个人中心和成绩查询页面。
- MySQL 用户、加密教务凭据和应用会话持久化。
- EAMS Session 失效后的单次自动重登。

当前还没有：

- Redis
- 课表及其他校园工具后端
- 完整业务实现

未收到用户明确需求时，不扩展完整后端系统。

## 3. 当前最高优先级编码规则

后续写代码时必须遵守：

1. 只实现当前需求，不预设未来需求。
2. 遵守最小职责原则：一个函数只做一件事。
3. 不添加用户没要求的安全校验、权限系统、缓存、限流、重试、日志框架、抽象层。
4. 只有输入来自外部用户、网络请求、数据库、文件系统时，才做必要校验。
5. 不为了“健壮性”写大量 if/else 兜底逻辑。
6. 不过度封装，不提前设计接口、工厂、策略模式。
7. 优先写直观、短小、可读的代码。
8. 如果认为需要额外安全判断，先用注释说明原因，不直接实现。
9. 每个文件控制在合理长度，函数超过 50 行要拆分。
10. 中文注释只解释业务意图，不解释显而易见的代码。
11. 不知道或无法从现有证据确认的信息，必须先询问用户，不得自行推测后直接修改代码。

本项目是小型工程项目，不写大厂级防御式代码。除非用户明确要求，不引入复杂架构、复杂安全判断和过度抽象。

### 3.1 外部系统事实确认规则

涉及 CAS、EAMS、一网通办等真实外部系统时，必须遵守：

1. 只把真实请求结果、用户提供的信息和当前代码行为作为实现依据。
2. 未看到认证后响应时，不得根据公开页面、前端代码或模拟测试推测认证后的响应结构。
3. 缺少状态码、响应字段、跳转方式、Cookie 名称等关键信息时，先向用户说明缺少什么，并请求用户提供实际运行结果。
4. 模拟单元测试只能验证本地解析和流程逻辑，不能作为学校真实登录链路已经可用的证据。
5. 不得连续提交多个猜测性修复。应先补充最小诊断信息，确认根因后再修改实现。
6. 回答和执行时明确区分“已确认事实”“尚未确认信息”和“推测”；未经用户确认，不得把推测写成既定事实。

## 4. 总原则

AI 在生成、修改、重构代码时，必须遵守：

1. 优先保证代码正确性，而不是追求炫技。
2. 优先保证项目结构清晰，而不是把功能堆在一起。
3. 优先保证可维护性，而不是一次性写完所有功能。
4. 优先使用简单直接的实现，不要过度设计。
5. 每次修改都要控制影响范围，避免无关改动。
6. 不允许为了“看起来高级”引入复杂架构、复杂抽象或不必要依赖。
7. 涉及真实学校系统、账号、密码、Cookie、Token 时，默认采用最保守方案。

本项目的核心开发原则是：

```text
简单、清晰、安全、稳定、可维护
```

## 5. 第一阶段开发目标

第一阶段只做最小闭环：

1. 初始化 Go module。
2. 实现 `pkg/jwxt`。
3. 实现 CAS/EAMS 登录链路检查。
4. 实现一网通办 API 登录并回跳 EAMS。
5. 实现安全命令行 demo。
6. 实现一网通办登录路由解析单元测试。

登录链路：

```text
GET http://jwgl.cuit.edu.cn/eams/
    ↓
EAMS 302
    ↓
https://sso.cuit.edu.cn/authserver/login?service=...
    ↓
CAS 返回包含一网通办登录入口的 HTML
    ↓
解析 loginType、redirectUrl
    ↓
POST 一网通办 /api/base/login
    ↓
一网通办返回回跳 EAMS 的 redirect_uri
    ↓
302 回跳 EAMS，携带一次性 ticket
    ↓
EAMS 验证 ticket
    ↓
EAMS 发 JSESSIONID
    ↓
后续访问课表、成绩、考试页面
```

## 6. 目录结构规则

本项目目录结构以 `architecture_guide.md` 为准，采用按业务域分包的模块化单体。

第一阶段代码优先放这里：

```text
pkg/jwxt/
cmd/jwxt-demo/
testdata/
```

后续业务系统再使用：

```text
apps/api/
apps/worker/
internal/bootstrap/
internal/platform/
internal/user/
internal/schedule/
internal/jwxtsync/
internal/course/
internal/favorite/
internal/academic/
internal/exam/
internal/notice/
migrations/
configs/
deploy/
```

目录职责：

| 目录 | 职责 |
|---|---|
| `pkg/jwxt` | CAS/EAMS 登录、Cookie 会话、教务页面访问、HTML 解析 |
| `cmd/jwxt-demo` | 本地手动验证 JWXT 登录链路 |
| `internal/jwxtsync` | 后续业务侧限流、锁、缓存编排 |
| `internal/schedule` | 后续课表业务域 |
| `internal/academic` | 后续成绩/GPA 业务域 |
| `internal/exam` | 后续考试安排业务域 |
| `internal/platform` | 后续 auth/cache/middleware/response 等基础能力 |

`pkg/jwxt` 不能依赖仓库根目录的 `internal/` 业务代码。

但 `pkg/jwxt` 可以有自己的私有实现目录，例如 `pkg/jwxt/internal/login/`。这是 Go 的 `internal` 可见性机制，用来防止外部业务层直接调用 SDK 内部登录细节。

说明：如果其他规范中出现 `handler/service/repository/client/parser/model` 这种按层目录示例，在本项目中应理解为职责分层，而不是要求改成按层目录。实际目录仍按当前架构文档的业务域组织。

### 5.1 `pkg/jwxt` 内部分层

`pkg/jwxt` 是 SDK 的边界，不代表把所有逻辑写到一起。

`pkg/jwxt` 内部仍然必须继续分层、分文件、分职责：

```text
pkg/jwxt/
├── client.go          # 对外 Client、NewClient、IsLoggedIn 等入口
├── config.go          # EAMS/CAS 地址、超时、User-Agent 等配置
├── errors.go          # 可 errors.Is 判断的 SDK 错误
├── grade.go           # 学期和成绩查询入口
├── internal/
│   ├── grade/          # 学期请求、成绩请求和纯解析
│   └── login/          # CAS/EAMS 登录私有实现
│       ├── flow.go     # 登录编排
│       ├── portal.go   # 一网通办登录路由解析和 API 登录
│       ├── redirect.go # 手动重定向处理，避免泄露 ticket/完整 URL
│       ├── session.go  # EAMS 会话确认、Session 失效判断
│       ├── detect.go   # CAS/Portal 登录页识别、host 判断
│       ├── inspect.go  # 登录流程安全检查
│       └── portal_test.go
```

分层规则：

1. `client.go` 只做 SDK 对外入口和依赖组装，不写大量登录细节。
2. `pkg/jwxt/internal/login/portal.go` 只处理一网通办登录路由解析和 API 登录。
3. `pkg/jwxt/internal/login/flow.go` 只编排登录流程，不解析课表、成绩、考试。
4. `pkg/jwxt/internal/login/redirect.go` 只处理 301/302/303/307/308 等跳转策略。
5. `pkg/jwxt/internal/login/session.go` 只判断 EAMS 会话是否有效。
6. `pkg/jwxt/internal/login/detect.go` 只放登录页识别和 host 判断，避免每个流程各写一套判断。
7. `pkg/jwxt/internal/grade/parser.go` 只做纯解析，不处理登录、不持有 Cookie、不访问网络。

外部调用者只应该使用 `pkg/jwxt.Client`，不要直接接触 `internal/login`。

不要因为“分层”过早拆出大量小包；也不要因为“最小版本”把无关职责塞进一个文件。

## 7. 最小职责原则

每个文件、函数、结构体、模块都只做一件明确的事情。

职责边界：

| 层 | 职责 |
|---|---|
| handler | 接收请求、参数校验、返回响应 |
| service | 业务逻辑编排 |
| repository | 数据库访问 |
| client | 调用外部系统，例如教务系统 |
| parser | 解析 HTML、JSON、XML 等外部数据 |
| model | 定义数据结构 |
| middleware | 鉴权、日志、跨域等中间件 |
| config | 配置读取 |

禁止行为：

1. handler 中直接写数据库查询。
2. handler 中直接调用教务系统。
3. repository 中写业务判断。
4. service 中拼接复杂 SQL。
5. parser 中处理用户权限。
6. 一个文件放多个不相关业务模块。
7. 一个函数同时完成登录、抓取、解析、入库、返回。

## 8. 中文注释规范

所有关键代码必须使用中文注释。

必须写中文注释的地方：

1. 核心业务逻辑。
2. 复杂条件判断。
3. 数据库查询逻辑。
4. 第三方接口调用逻辑。
5. 登录、认证、权限相关逻辑。
6. 教务系统数据解析逻辑。
7. 定时任务、缓存、重试、异常处理逻辑。
8. 容易让后续开发者误解的代码。

注释必须解释“为什么这样做”，不要只是翻译代码。

错误示例：

```go
// 判断是否为空
if user == nil {
    return nil
}
```

正确示例：

```go
// 教务系统可能返回空用户信息，这里提前拦截，避免后续解析课表时出现空指针
if user == nil {
    return nil
}
```

禁止写无意义注释：

```go
// 定义变量
// 调用方法
// 返回结果
```

## 9. 命名规范

命名必须清晰表达含义，禁止使用模糊命名。

推荐：

```text
userService
scheduleService
examRepository
jwxtClient
scheduleParser
```

禁止：

```text
data
info
result
temp
doSomething
handleData
test
aaa
```

如果变量含义明确，可以使用短变量名，例如：

```text
id
err
ctx
req
resp
```

但不能为了省事牺牲可读性。

## 10. 安全红线

任何代码、日志、测试、终端输出都不能泄露：

- 密码。
- Cookie 值。
- JSESSIONID 值。
- CAS ticket 值。
- Token。
- Session。
- 密钥。
- 身份证号。
- 学号加密码的组合信息。
- 完整 `service` 参数。
- 完整重定向 URL。
- CAS 登录页 HTML 正文。
- EAMS 页面 HTML 正文。

允许输出的只有安全摘要：

- host。
- path。
- HTTP 状态码。
- 一网通办登录入口 host/path。
- 一网通办登录字段名摘要。

不要打印 query string。

不要打印响应 body。

不要把原始网络错误直接返回给用户，因为其中可能包含完整 URL。

禁止行为：

1. 禁止明文存储用户密码。
2. 禁止在日志中输出密码、Cookie、Token。
3. 禁止把密钥写死在代码里。
4. 禁止把 `.env`、配置密钥提交到仓库。
5. 禁止在前端保存长期有效的敏感 Token。
6. 禁止绕过权限校验直接返回用户数据。
7. 禁止一个用户访问另一个用户的数据。

必须做到：

1. 密码、Token、密钥必须通过环境变量或安全配置读取。
2. 用户数据查询必须带上当前用户 ID。
3. 接口必须进行身份校验。
4. 敏感字段返回前必须过滤。
5. 外部请求必须设置超时时间。
6. 重要操作必须有错误处理和日志。

## 11. CookieJar 规则

每个用户、每次独立登录会话都必须使用独立 CookieJar。

禁止：

- 全局共享 CookieJar。
- 全局共享已登录 Client。
- 跨用户复用 CAS/EAMS Cookie。

原因：

CAS Cookie、EAMS JSESSIONID 和一次性 ticket 都属于某个用户的认证上下文。共享 CookieJar 会导致会话串号、身份混淆和隐私泄露。

## 12. JWXT SDK 实现要求

第一阶段 SDK 应使用：

- `github.com/go-resty/resty/v2`
- `net/http/cookiejar`
- `github.com/PuerkitoBio/goquery`
- `golang.org/x/term`

Client 初始化要求：

- 创建独立 CookieJar。
- Resty 使用该 CookieJar。
- 超时 15 秒。
- 禁止自动重试。
- 设置正常浏览器 User-Agent。
- 设置 Accept。
- 默认不启用 Debug。

登录成功判断不能只看 HTTP 200。

至少需要确认：

- 最终 URL 不在 `sso.cuit.edu.cn/authserver/login`。
- 再次访问 EAMS 首页或受保护页面时，不再跳回 CAS。
- 响应中不出现 `authserver/login` 或 `loginType=cas`。

教务系统属于外部不稳定系统，必须考虑：

1. 教务系统超时。
2. 教务系统返回 302 跳转。
3. Cookie 失效。
4. Session 过期。
5. HTML 结构变化。
6. 登录失败。
7. 验证码或额外认证。
8. 返回空页面。
9. 返回乱码。
10. 学校系统临时维护。

请求规范：

1. 所有请求必须设置超时时间。
2. 不允许无限重试。
3. 重试必须有最大次数。
4. 不允许高频请求教务系统。
5. 必须避免对学校系统造成压力。
6. 可以缓存的数据应优先缓存。

## 13. 错误处理

所有可能失败的操作都必须处理错误，包括：

1. 数据库操作。
2. 网络请求。
3. JSON 解析。
4. HTML 解析。
5. 类型转换。
6. 配置读取。
7. 第三方系统返回异常。

错误信息必须有上下文，不能直接丢失错误原因。

推荐：

```go
return fmt.Errorf("获取课表失败: %w", err)
```

禁止：

```go
result, _ := client.Fetch()
```

除非有明确中文注释说明为什么可以忽略。

SDK 至少保留这些错误类型：

```text
ErrRemoteUnavailable
ErrUnsupportedLoginPage
ErrInvalidCredentials
ErrLoginVerificationFailed
ErrSessionExpired
```

SDK 错误要求：

- 错误必须可用 `errors.Is` 判断。
- 错误文本只能包含安全上下文。
- 可以包含操作名、host、path、状态码。
- 不要包含密码、Cookie、ticket、query、HTML body。

## 14. 日志规范

日志必须服务于排查问题，不允许乱打日志。

必须记录日志的情况：

1. 用户登录失败。
2. 教务系统请求失败。
3. 数据解析失败。
4. 数据库写入失败。
5. 定时任务执行失败。
6. 关键业务流程异常。

日志中禁止出现：

1. 用户明文密码。
2. Cookie。
3. Token。
4. Session。
5. 身份证号。
6. 学号加密码的组合信息。
7. 任何敏感认证字段。

错误示例：

```go
log.Println("用户密码:", password)
```

正确示例：

```go
log.Println("教务系统登录失败，用户ID:", userID)
```

## 15. 数据解析规范

解析教务系统返回的数据时，必须单独放到 parser 层或 `pkg/jwxt` 内部的解析文件中。

禁止在 service 或 handler 中直接写大量 HTML 解析逻辑。

解析逻辑要求：

1. 字段缺失时不能直接崩溃。
2. HTML 结构变化时要返回明确错误。
3. 解析结果必须使用结构体表达。
4. 不允许直接把原始 HTML 返回给前端。
5. 复杂解析规则必须写中文注释。

## 16. 数据库规范

数据库操作必须通过 repository 层完成。

禁止行为：

1. handler 直接操作数据库。
2. service 拼接复杂 SQL。
3. SQL 字符串到处散落。
4. 不加条件地更新或删除数据。
5. 查询用户数据时不带用户 ID。

必须做到：

1. 查询用户数据必须校验归属。
2. 更新和删除必须带明确条件。
3. 数据库错误必须向上返回。
4. repository 不处理业务逻辑。
5. 表结构字段命名必须清晰。

## 17. API 接口规范

接口返回格式必须统一。

推荐格式：

```json
{
  "code": 0,
  "message": "success",
  "data": {}
}
```

错误返回示例：

```json
{
  "code": 40001,
  "message": "教务系统登录失败",
  "data": null
}
```

接口要求：

1. 参数必须校验。
2. 错误信息要明确。
3. 不直接暴露底层错误给前端。
4. 不返回敏感字段。
5. 接口命名要符合 REST 风格。
6. 同类接口风格必须统一。

## 18. 配置规范

配置必须和代码分离。

禁止写死：

1. 数据库地址。
2. Redis 地址。
3. 密钥。
4. Token。
5. 教务系统账号密码。
6. 后端服务地址。
7. 第三方 API Key。

推荐使用：

- `.env`
- `config.yaml`
- 环境变量

但敏感配置文件不能提交到 Git 仓库。

## 19. 前端规范

如果项目包含前端，必须遵守：

1. 页面结构清晰。
2. 组件职责单一。
3. 不在页面组件中堆积大量业务逻辑。
4. API 请求统一封装。
5. 错误提示统一处理。
6. 登录态统一管理。
7. 不在前端写死后端地址。
8. 不在前端保存敏感信息。
9. UI 优先简洁实用，不追求复杂动画。

移动端采用 iOS 风格时，还必须遵守：

1. 页面背景优先使用低饱和系统灰和轻微渐变，避免大面积鲜艳颜色。
2. 卡片使用半透明浅色材质，并通过 `backdrop-filter` 营造磨砂效果；同时提供不支持模糊时仍可读的背景色。
3. 卡片边缘使用约 `0.5px` 的浅色高光边，禁止使用生硬的深色粗描边。
4. 阴影使用低透明度、多层次柔和投影，避免明显悬浮和厚重黑影。
5. 同类卡片统一圆角，移动端内容卡片建议保持在 `18px` 至 `24px`。
6. 分组列表优先使用一个外层卡片和内缩分隔线，不为每一行重复添加完整边框。
7. 主色保持克制；错误、挂科等异常状态才使用红色，并以文字或轻微底色提示，避免高饱和色块。
8. 页面背景、状态栏主题色和安全区颜色必须连续，不能在顶部形成明显分割线。
9. 信息密度应紧凑但可读，间距、字号、圆角和图标尺寸在同一页面保持一致。
10. 动效只用于状态反馈，时长保持简短，不使用夸张弹跳、强烈缩放或大范围渐变动画。

## 20. 依赖规范

引入新依赖必须谨慎。

引入依赖前必须考虑：

1. 标准库能否解决。
2. 现有依赖能否解决。
3. 新依赖是否稳定。
4. 新依赖是否会增加项目复杂度。
5. 新依赖是否真的必要。

禁止因为一个很小的功能引入大型依赖。

第一阶段已经明确允许的依赖：

- `github.com/go-resty/resty/v2`
- `github.com/PuerkitoBio/goquery`
- `golang.org/x/term`

## 21. 测试策略

默认测试必须是离线测试。

第一阶段必须写纯单元测试验证：

- 一网通办 hash 路由提取。
- 一网通办 history 路由提取。
- `redirectUrl` 不在最后一个 query 参数时也能提取。

核心业务逻辑应尽量编写测试，优先测试：

1. 课表解析。
2. 成绩解析。
3. 考试信息解析。
4. 登录流程。
5. 权限校验。
6. 数据库查询条件。
7. 异常情况处理。

真实学校系统测试必须显式开启：

- 使用 build tag；或
- 使用环境变量。

禁止在测试代码中写真实学号、密码、Cookie、ticket。

## 22. Git 提交规范

每次提交应只包含一个明确主题。

推荐格式：

```text
feat: 新增课表查询接口
fix: 修复教务系统登录失败问题
refactor: 拆分课表解析逻辑
docs: 补充项目说明文档
chore: 调整配置文件
```

禁止一次提交混入大量无关修改。

## 23. 开发命令

写 Go 代码后常用验证：

```bash
go mod tidy
go test ./...
go vet ./...
```

前端代码位于 `apps/web`，常用验证：

```bash
cd apps/web
pnpm run typecheck
pnpm run build
```

如果只改文档或目录结构，不需要运行 Go 测试。

## 24. AI 修改代码流程

AI 每次修改代码时，必须遵守：

1. 先理解现有项目结构。
2. 优先复用已有代码。
3. 不随意新建重复模块。
4. 不随意更改公共结构体。
5. 不随意修改接口返回格式。
6. 不随意引入新依赖。
7. 修改范围必须尽量小。
8. 修改后必须检查是否影响其他模块。
9. 如果发现历史代码有问题，只修改和当前任务相关的部分。
10. 不允许进行无关重构。

执行任务时遵守以下顺序：

1. 先看当前仓库状态。
2. 再读相关文档。
3. 确认任务属于哪个阶段。
4. 只改任务范围内的文件。
5. 不顺手实现未被要求的功能。
6. 涉及凭据、安全、真实学校系统时，默认保守处理。
7. 完成后说明改了什么、没改什么、如何验证。

生成代码前必须先判断：

1. 这个功能应该放在哪一层？
2. 有没有违反最小职责原则？
3. 有没有泄露敏感信息？
4. 有没有写中文注释？
5. 有没有处理错误？
6. 有没有引入不必要复杂度？
7. 有没有修改无关代码？
8. 后续维护者是否容易看懂？

如果不满足以上要求，必须先调整设计，再生成代码。

## 25. 明确不要做

除非用户明确要求，否则不要做：

- 抢课功能。
- 批量请求学校系统。
- 自动续登。
- 保存教务密码。
- 保存 Cookie。
- 接入 Redis。
- 接入 MySQL。
- 接入 Hertz。
- 上 Docker Compose。
- 上 Cloudflare Tunnel。
- 写前端。
- 微服务架构。
- 分布式事务。
- 复杂领域驱动设计。
- 事件溯源。
- CQRS。
- 复杂消息队列架构。
- 多级缓存体系。
- 过早抽象接口。
- 过度封装工具类。
- 为了扩展性牺牲当前可读性。

本项目要从最小可验证链路开始，稳定后再向外扩展。

如果用户说“先不要写代码”，只能改文档、目录、计划，不创建 `.go` 实现文件。

如果用户说“开始写 SDK”，优先从 `pkg/jwxt` 的表单解析和 Client 骨架开始。

如果用户说“接 API”，再进入 `apps/api`、`internal/bootstrap`、`internal/platform`。
